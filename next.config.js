/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "imnpkveaeshutdtrzujw.supabase.co",
      },
    ],
  },
};

module.exports = nextConfig;
